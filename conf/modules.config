/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode,
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]

    // Prepare inputs
    
    withName: 'FASTP' {
        publishDir = [ 
            path: "${params.outdir}/fastp",
            pattern: "*{html,log,json}"
            ]
    }
    withName: 'UNZIP_FASTA' {
        publishDir = [ enabled: false]
    }
    withName: 'SAMTOOLS_FAIDX' {
        publishDir = [ 
            path: "${params.outdir}/reference_genome",
            mode: params.publish_dir_mode,
            pattern: "*{fa,fasta,fai}"
        ]
    }
    withName: 'BWAMEM2_INDEX' {
        publishDir = [ 
            path: "${params.outdir}/variantcalling",
            mode: params.publish_dir_mode,
            pattern: "bwamem2",
            saveAs: { filename -> 'bwamem2_index'}
        ]
    }
    withName: 'GATK4_CREATESEQUENCEDICTIONARY' {
        publishDir = [ enabled: false]
    }

    // Alignment for variantcalling
    
    withName: 'BWAMEM2_MEM' {
        publishDir = [ enabled: false]
            // Mark shorter splits as secondary (-M), add read groups to bam (-R),
            // and process in batches of (-K) bases for better reproducibility 
        ext.args   = { "-M -K 100000 -R ${meta.read_group}" }
        ext.args2  = { "-n" }
    }

    // Classification by aligning to composite reference

    withName: 'COMBINE_GENOMES' {
        publishDir = [ enabled: false ]
    }    
    withName: 'COMPOSITE_INDEX' {
        publishDir = [ enabled: false ]
    }
    withName: 'COMPOSITE_ALIGN'{
        publishDir = [ 
            path: "${params.outdir}/competitive_alignment/bwamem2_mem",
            pattern: "*{cram,crai,bam,bai}",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
            /* 
            Args for bwa-mem2 mem: drop if seed is smaller than 5 (-W)
                and process in batches of (-K) for better reproducibility   */
        ext.args   = { "-K 100000 -W 5 " }
    }
    withName: 'EXTRACT_MAPPING_STATS' {
        publishDir = [ enabled: false ]
    }
    withName: 'CONCATENATE_MAPPING_STATS' {
        publishDir = [ 
            path: "${params.outdir}/competitive_alignment/summary",
            pattern: "*summary.tsv",
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
            ]
    }

    // BAM processing or STATS
    
    withName: 'GATK4SPARK_MARKDUPLICATES' {
        publishDir = [
            path: "${params.outdir}/variantcalling/deduplicated_bams/",
            pattern: "*{cram,bam}",
            mode: params.publish_dir_mode
        ]
    }
    withName: 'SAMTOOLS_INDEX' {
        publishDir = [
            path: "${params.outdir}/variantcalling/deduplicated_bams/",
            pattern: "*{crai,bai}",
            mode: params.publish_dir_mode
        ]
    }
    withName: 'SAMTOOLS_STATS' {
        publishDir = [ path: "${params.outdir}/variantcalling/alignment_stats/samtools"]
    }
    withName: 'SAMTOOLS_REINDEX_BAM' {
        // high mapq , primary read paired properly mapped
        ext.args    = { ' -F 3844 -q 30 ' }
        // only used for goleft_indexcov, not a meaningful output
        publishDir  = [ enabled: false ]     
    }
    withName: 'GOLEFT_INDEXCOV' {
        publishDir = [
            mode: params.publish_dir_mode,
            path: { "${params.outdir}/variantcalling/alignment_stats" },
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
            ]
        ext.prefix = { ${meta.organism} }
        ext.args = {
            (   params.indexcov_options ?: '' ) 
        }
    }
    withName: 'MOSDEPTH' {
        cpus = 4
        publishDir = [
            path: "${params.outdir}/variantcalling/alignment_stats/mosdepth",
            pattern: "*{txt,tsv}",
            mode: params.publish_dir_mode
        ]
    }

    // Variant calling
    
    withName: 'BCFTOOLS_MPILEUP' {
        ext.args    = { params.opt_bcfpileup ? "${params.opt_bcfpileup}" : '' }
        ext.args2   = { 
            ( params.consensus_caller ? '--consensus-caller' : '--multiallelic-caller') +
            " --ploidy 1 " }
        ext.args3   = { "-i 'count(GT==\"RR\")==0'" } // only report non homozygous reference variants
        publishDir  = [
            [
                path: "${params.outdir}/variantcalling/bcftools",
                pattern: "*.vcf*",
                mode: params.publish_dir_mode
            ],
            [
                path: "${params.outdir}/mpileup",
                pattern: "*.mpileup*",
                mode: params.publish_dir_mode
            ]
        ]
    }
    withName: 'FREEBAYES' {
        ext.args   = { "--ploidy ${params.ploidy} --min-alternate-fraction 0.05 --min-mapping-quality 1" }
        publishDir = [
            enabled: false
        ]
    }
    withName: 'BCFTOOLS_SORT' {
        ext.prefix = { meta.id + ".freebayes" }
        publishDir = [
            mode: params.publish_dir_mode,
            path: { "${params.outdir}/variantcalling/freebayes" },
            pattern: "*vcf.gz"  ]
    }
    withName: 'GATK4_HAPLOTYPECALLER' {
        ext.args   = { 
            ( params.haplotype_caller_options ?: "" ) +
            " --sample-ploidy ${params.ploidy} " 
            }
        ext.prefix = { "${meta.id}_haplotypecaller" }
        publishDir = [ enabled: false ]
    }
    withName: 'GATK4_VARIANTFILTRATION' {
        ext.args   = { paramns.variantfiltration_options ?: '' }
        ext.prefix = { "${meta.id}_varfiltration" }
        publishDir = [
            mode: params.publish_dir_mode,
            path: { "${params.outdir}/variantcalling/haplotypecaller"},
            pattern: "*{vcf.gz,tbi}"
        ]
    }
    
    // VCF stats

    withName: 'BCFTOOLS_STATS' {
        ext.prefix = { vcf.baseName - ".vcf|.vcf.gz" }
        publishDir = [
            mode: params.publish_dir_mode,
            path: { "${params.outdir}/vcfreports/${meta.variantcaller}/" },
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: 'VCFTOOLS_QUAL' {
        ext.prefix = { variant_file.baseName.replaceFirst(/\.vcf(\.gz)?$/, '') }
        ext.args   = { '--TsTv-by-qual' }
        publishDir = [
            mode: params.publish_dir_mode,
            path: { "${params.outdir}/vcfreports/${meta.variantcaller}/" },
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: 'VCFTOOLS_SUMMARY' {
        ext.prefix = { variant_file.baseName.replaceFirst(/\.vcf(\.gz)?$/, '') }
        ext.args   = { '--FILTER-summary' }
        publishDir = [
            mode: params.publish_dir_mode,
            path: { "${params.outdir}/vcfreports/${meta.variantcaller}/" },
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
}
